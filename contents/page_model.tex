\input{headers/headerpage}

%
% To make a new page, create a new directory in /contents, in uppercase.
% put this file and rename it page.tex
%

% MEMO
%
% Add title
%\addtitle{COMMANDS :}
%
% Generate a tables of command with description
%\begin{tabcmd}
%\addline{sed -n '/home/p' /etc/passwd}{search home string in passwd file and print it} 
%\end{tabcmd}
%
% Add syntax highlighting
%\begin{code}{Test code}{Python}
%import os,sys
%if A = B then:
%    print("this is ok")
%else:
%    sys.exit()
%\end{code}

% REMEBER:
% \# \& for these characters 

% Configure syntax highlighting for all the document, parameter must be empty in code env
%\lstset{language=Python}

\begin{document}
\end{document}
